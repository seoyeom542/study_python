import numpy as np

print(np.zeros(3)) # 0이 3개 들어있는 배열 생성

print('\n# zeros((2,2)) : 요소의 값이 모두 0인 2 * 2의 배열 생성')
arrZeros = np.zeros((2,2)) # 2차원 배열 생성  
print(arrZeros)

print('\n# ones((3,2)) 배열 생성')
array2 = np.ones((3,2)) # 3열, 2행
print(array2)